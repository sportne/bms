/**
 * Root package for the BMS compiler.
 *
 * <p>This package contains the pipeline entry point that runs validation, parsing, semantic
 * resolution, and then hands off to generators.
 */
package io.github.sportne.bms;
