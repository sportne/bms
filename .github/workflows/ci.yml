name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Install C++ compiler
        run: |
          sudo apt-get update
          sudo apt-get install --yes g++ clang-tidy cppcheck

      - name: Verify C++ compiler
        run: g++ --version

      - name: Verify C++ analyzers
        run: |
          clang-tidy --version
          cppcheck --version

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Fail on Gradle deprecations
        run: ./gradlew help --warning-mode fail --console=plain --no-configuration-cache --no-daemon

      - name: Run Gradle check
        run: ./gradlew check --warning-mode all --console=plain --no-daemon

  native:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up GraalVM 21
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: graalvm-community
          java-version: "21"
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: "true"

      - name: Export GRAALVM_HOME
        run: echo "GRAALVM_HOME=${JAVA_HOME}" >> "$GITHUB_ENV"

      - name: Verify native-image
        run: native-image --version

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Build native image
        run: ./gradlew :app:nativeCompile --no-daemon --no-configuration-cache
